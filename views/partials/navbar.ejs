<!-- partials/navbar.ejs -->
<nav>
  <ul class="navbar">
    <li><a href="/" class="navbar-link">Home</a></li>
    <li><a href="/users" class="navbar-link">Usuarios</a></li>
    <li><a href="/auth" class="navbar-link">Autenticación</a></li>
  </ul>
</nav>

<style>
  /* Estilo del navbar */
  nav {
    background-color: #5c6bc0; /* Fondo del navbar */
    padding: 10px 20px; /* Espaciado interno */
    border-bottom: 2px solid #3f51b5; /* Borde inferior para darle separación */
  }

  /* Lista dentro del navbar */
  .navbar {
    list-style: none; /* Elimina los puntos de la lista */
    display: flex; /* Para que los elementos se alineen horizontalmente */
    justify-content: center; /* Centra los elementos de la lista */
    margin: 0;
    padding: 0;
  }

  /* Elementos de la lista */
  .navbar li {
    margin: 0 20px; /* Espaciado entre los enlaces */
  }

  /* Enlaces del navbar */
  .navbar-link {
    color: white;
    text-decoration: none;
    font-size: 18px;
    padding: 10px 15px;
    border-radius: 5px;
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  /* Cambiar color de los enlaces al pasar el mouse */
  .navbar-link:hover {
    background-color: #3f51b5;
    color: #fff;
  }

  /* Estilos responsivos */
  @media (max-width: 768px) {
    .navbar {
      flex-direction: column; /* Los elementos del navbar se apilan verticalmente en pantallas pequeñas */
    }

    .navbar li {
      margin: 10px 0;
    }
  }
</style>

  
