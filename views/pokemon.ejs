<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pokemon</title>
</head>
<body>
  <% if (pokemon) { %>
    <h1><%= pokemon.name %> (#<%= pokemon.id %>)</h1>
    <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>">
  <% } else { %>
    <p>Error: <%= error %></p>
  <% } %>
</body>
</html>
